# GPU server: receive H.264, decode, run vision (ONNX/CUDA) + attention calc.
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0

# GPU inference. Requires a matching CUDA/cuDNN; falls back to CPU provider.
onnxruntime-gpu>=1.17

# H.264 decode.
av>=11.0

# YuNet detector + image ops.
opencv-python>=4.9.0
numpy>=1.24.0
